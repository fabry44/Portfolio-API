{% extends '@!EasyAdmin/layout.html.twig' %}


		{% block title %}
			Tableau de Bord
		{% endblock %}

		{% block content_title '<h1><span class="sospro">Tableau de Bord</span></h1>' %}

		{% block javascripts %}
			{% block importmap %}
			
				
			{% endblock %}
		{% endblock %}

		{% block main %}

			<div class="container">
				<div class="row g-3 mb-5">
					<div class="col-12 col-md-4 d-flex align-items-center justify-content-center p-0">
						<a href="{{ path('api_update_portfolio') }}" class="btn btn-info">Mettre à jour le Portfolio</a>
						<a href="{{ path('api_netlify_build') }}" class="btn btn-success">Lancer le Build</a>
					</div>
					<div class="col-12 col-md-4 d-flex align-items-center justify-content-center p-0">
					
				</div>
			</div>
	²
		{% endblock %}